Homepage Oxipack Showreel Integration Report

Scope
- Updated only the Homepage proof section containing the DMN testimonial and Oxipack case-study card.
- Replaced the previous Oxipack media placeholder with the real Oxipack showreel used by the Oxipack Case Study page.
- Did not change Homepage copy, layout, spacing, typography, testimonial, stats, CTA, header, footer, or Case Study pages.

Homepage Component Changed
- app/page.tsx
- components/OxipackProofVideo.tsx
- app/globals.css, scoped .proof-video rules only

Existing Oxipack R2 Source Reused
- https://pub-d0adc0fc26c84d8e8c8db97d1ab2d30f.r2.dev/nexubis/oxipack/Oxipack%20Specific%20Showreel.mp4

Exact Poster Reused
- /assets/work/oxipack/hero-poster.webp

Source of Truth
- The Homepage now references Oxipack heroVideo through getCaseStudyBySlug("oxipack") from lib/work/data.ts.
- No second hard-coded video source was introduced in app/page.tsx.
- The Oxipack Case Study source remains unchanged.

Playback Behaviour
- The existing Homepage card showed a central play UI and had no autoplay wiring, so the implementation keeps the intended click-to-play interaction.
- Video starts muted.
- Video uses playsInline.
- Video uses preload="metadata".
- Native browser controls are disabled.
- The central play button is a real button element.
- The play button hides visually while playback is active.
- Clicking again pauses/replays through the same accessible control.

Audio-Control Behaviour
- No separate mute/unmute control was added because the existing Homepage proof card did not have a visible audio-control design.
- The video remains muted.
- The Homepage proof video keeps its own independent video ref and playback state.
- It does not share state with the Homepage main showreel or Case Study videos.

CTA
- The existing Read the full case study CTA remains unchanged.
- Destination remains internal: /work/oxipack.

Poster and Fallback
- The approved Oxipack poster appears before playback and during initial loading.
- On genuine video error, the component leaves the project-specific poster available and logs a concise development error.
- No generic reel_draft image remains in this proof-card media slot.

Responsive Verification
- Checked at: 1440px, 1280px, 1024px, 991px, 767px, 390px, 375px.
- Confirmed the media stays inside the existing proof-video frame.
- Confirmed no horizontal overflow at checked widths.
- Confirmed CTA remains /work/oxipack.
- Confirmed no Webflow media request for the updated card.
- Confirmed no console errors during browser checks.

Validation Commands
- npm run lint: passed with existing warnings.
- npm run typecheck: passed.
- npm run test: passed, 30 files / 233 tests.
- npm run build: passed.

Known Existing Warnings
- npm run lint still reports existing no-img-element warnings and one unused eslint-disable warning outside this scoped change.
- npm run build still reports the existing DMN placeholder proof-content warning.

Files Changed for This Task
- app/page.tsx
- app/globals.css
- components/OxipackProofVideo.tsx

Working Tree Note
- The working tree already contained unrelated About/blog changes before this report was created.
- Those unrelated changes were not modified for this Homepage Oxipack showreel integration.
