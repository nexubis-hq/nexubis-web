# Nexubis

> Look as credible as your engineering, with one in-house creative team.

Nexubis is the in-house creative team for European industrial manufacturers whose product is better than their brand shows. This site sells that positioning: brand, website, 3D, video, and print on one flat monthly retainer.

## Brand Identity

- Personality: confident, industrial, premium, plain-spoken. Sells on outcomes, not adjectives.
- Colours (existing tokens, unchanged): red accent `#FF4141`, near-black `#1d1c1a`, body grey `#5a5a5a`, off-white surfaces `#f2f2f2`, deep "work black" `#0f0f0f` for the dramatic dark sections.
- Fonts (existing, unchanged): Helvetica Now Display for headings and confident lines, Inter for body.
- Motion: subtle. A hero intro sequence plus a gentle "lift and fade" as each section scrolls in. Everything respects reduced-motion.

## Pages

- **Homepage** (`/`) - The locked 12-section story. See below.
- **Packages** (`/packages`) - Pricing and packages page (unchanged this round; it shares the header and footer).
- **Scorecard** (`/scorecard`) - The Industrial Brand Credibility Scorecard: the instant lead-generator tool. A visitor enters **only their website** - nothing else. The tool reads their site, works out what they make and who their competitors are on its own, checks all of them (crawl, first-impression screenshots, loading speed, web presence), scores 25 checks with Nexubis AI, and shows a preview on the spot. The page uses the same header as the homepage, and the entry card shows a preview of the five areas we look at (Brand, Website, Visuals, Print, Message). Entering name, work email and role unlocks the full report.
- **Shared reports** (`/scorecard/r/...`) - Each unlocked report lives at its own private link for 180 days: score, competitor benchmark, five category pages, the first place to fix, the Oxipack proof, and the booking step. Not indexed by search engines. A personal video can be attached from the admin and appears at the top.
- **Scorecard admin** (`/scorecard/admin`) - Password-protected team area: the leads table (with a "Loom candidates" view for the weekly video selection), notes, and per-report tools (attach the video, regenerate, copy the link). Not indexed.

## Homepage sections (in order)

1. **Hero** - Headline, sub-headline, the "Book an application call" button, the showreel video, and the client logo strip.
2. **Value stack** - Three outcome tiles: Buyers see your worth / More output, faster turnaround / Brand ready for every launch.
3. **Stakes** - A short dark narrative about what a weak brand costs a great product.
4. **The Guide** - An empathy line, our authority line, and three proof bullets. No client names or stats here.
5. **Solutions** ("What we take off your plate") - Five services in an accordion, each with a "Learn more" link: Brand identity, Website, 3D & CGI, Video & motion, Trade show & print.
6. **The Plan** ("How it works") - Three numbered steps, a summary paragraph, and the booking button.
7. **Brand Credibility Scorecard** - The lead-generator block. Hidden by default; see "The Scorecard switch" below.
8. **Proof** - One customer quote (DMN, currently a clearly marked placeholder to collect) and one case tile (Oxipack) linking to the case study.
9. **Success statement** - A vivid "picture the next trade show" paragraph and the booking button.
10. **Proof bar** - A slim band with three short promises.
11. **FAQ** - Seven questions in an accordion.
12. **Footer** - Links, social, and the sign-off "Built brilliantly. Branded to match."

## Components

- **Header** (`components/SiteHeader.tsx`) - Logo, navigation, and the "Book an application call" button. The Scorecard is not linked from the header; when it is switched on, the Credibility Check appears in exactly three places: the hero transitional link, Section 7, and the footer.
- **Footer** (`components/SiteFooter.tsx`) - Wordmark, link menu (About, What we do, Work / Case studies, Scorecard when on, Contact, Privacy & terms), social (LinkedIn, Instagram), and the sign-off line.
- **RevealOnScroll** (`components/RevealOnScroll.tsx`) - Drives the gentle scroll-in motion for the new sections.
- **HeroAnimations / FooterAnimations** - Existing motion for the hero and footer, kept as-is.

## The one place to change links: `lib/site-config.ts`

Every button and important link on the homepage reads from this one file, so you never hunt through the code:

- `BOOKING_URL` - Where "Book an application call" goes. **Now the real calendar: `cal.com/nexubis/30min`** (sourced from `lib/booking.ts`, the single place any cal.com link lives). Report pages use a smarter version that also carries the person's name, business, and report link into the booking form.
- `SCORECARD_URL` - The Scorecard tool (`/scorecard`). The tool ships later; the address is reserved now.
- `CONTACT_URL` - The contact page.
- `OXIPACK_CASE_URL` - The Oxipack case study link.
- `FOOTER_LINKS` / `SOCIAL_LINKS` - The footer menus.

## The Scorecard switch (a feature flag)

Everything about the Brand Credibility Scorecard is hidden until you turn it on. When on, it appears in exactly three places: Section 7, the hero's "Check your brand's credibility" link and support line, and the footer link. It is deliberately not in the header, so it never competes with the "Book an application call" button.

- **To turn it on:** set an environment variable `NEXT_PUBLIC_SHOW_SCORECARD` to `true`, then rebuild.
- **When off (the default):** the whole Section 7 block is completely absent (no empty gap), and the hero shows only the "Book an application call" button, laid out to look intentional that way.

## How to customise

- **Change any CTA destination:** edit `lib/site-config.ts`.
- **Change wording:** the homepage copy lives near the top of `app/page.tsx` as clearly named blocks (hero, value tiles, stakes, guide, solutions, plan, scorecard, proof, success, proof bar, FAQ).
- **Change colours or fonts:** edit the tokens at the top of `app/globals.css`. The whole site follows them.
- **Turn the Scorecard on or off:** the `NEXT_PUBLIC_SHOW_SCORECARD` switch above.

## The Scorecard tool (how it works, in plain words)

- **Where things live:** the engine is in `lib/scorecard/` (crawling, screenshots, scoring, emails, storage), the screens in `components/scorecard/`, and the pages under `app/scorecard/`. All the words a visitor sees are in one file, `lib/scorecard/copy.ts`.
- **What a run costs:** roughly 20 to 30 US cents in API fees per fresh check (measured, logged per run). Repeat checks of the same company are free thanks to caching.
- **Leads:** every unlock saves a lead (visible in the admin), pings the team by email, and sends the contact to Funnelr by webhook so the email sequence starts. If Funnelr is not connected yet, a built-in first email can be switched on with `SCORECARD_SEND_EMAIL1=true`.
- **House rules are enforced by tests:** no em dashes anywhere, the word "audit" never appears for visitors (it is always the Scorecard or the Credibility Check), Oxipack is named exactly once per report, and the AI only states what it actually saw.
- **Test drive without spending:** `SCORECARD_MOCK=1` makes the whole tool run on canned data (currently ON in local development). In production this must be OFF.
- **Before launch:** work through `LAUNCH_CHECKLIST.md` (new database, email domain, bot-check keys, Funnelr webhook, privacy wording).

## Recent Changes

- 2026-07-23: Turned on Meta and email, and started a migration checklist. **Meta:** the pixel (browser) and the server-side Conversions API are both live and verified — a real test event reached Meta (`events_received: 1`). The pixel ID was corrected to `885652097948360`. **Email:** Resend is now connected (a test email sent successfully), and new-lead alerts now go to **laine@nexubis.io** (was `hello@`), sent from `alerts@nexubis.io`. **New living doc `docs/go-live-and-migration.md`** lists every key/setting to configure and exactly what changes when the site moves to the real `nexubis.io` domain — built to make that move smooth and hard to get wrong. Still pending: the Funnelr CRM switch-over (waiting on Shannah's tag doc), Laine's cal.com event setup, Turnstile + database keys, and the Meta custom conversions (created in Meta's dashboard).
- 2026-07-23: Wired up the booking, tracking, and lead pieces of the Scorecard funnel (following the funnel playbook now saved at `docs/funnel-audit-checklist.md`). **Booking:** every "Book an application call" button now goes to the real calendar, `cal.com/nexubis/30min`, set in one place (`lib/booking.ts`); the buttons on a finished report also pass along the person's name, their business, and the report link so a booking is tied back to their Scorecard. A new secure endpoint (`/api/cal-booking`) listens for confirmed bookings from cal.com. **Meta (Facebook) tracking:** the Meta pixel is now live sitewide (id `885652097948360`), and the four funnel moments are tracked — starting a check, unlocking a report (a "Lead"), clicking to book, and a confirmed booking ("Schedule") — each also sent server-side for accuracy. The server side stays dormant (does nothing, no errors) until a Meta access token is added. **Funnelr:** fixed three real bugs in the code that talks to Funnelr (the CRM) so it will actually save contacts once switched on. The full switch-over of the live path onto the new clean Funnelr system is deliberately waiting for Shannah's Nexubis tag/scenario doc, so the exact tag names are right first time. **Note:** none of this sends real data yet until the matching keys are set in the hosting settings (cal.com secret, Meta token, Funnelr key) and Laine finishes the cal.com event setup.
- 2026-07-15: Fixed three consistency notes on the red "Value stack" pictures. (1) In "Buyers See Your Worth" the two cards are now exactly the same height — the plain "Built Without Strategy" card no longer sits taller — and the winning "Built By nexubis" card lifts straight up (with a red glow) instead of growing, so the sizes always match. (2) In "More Output, Faster Turnaround" the two card piles were nudged up so they sit centred in the frame rather than low. (3) "Brand Ready for Every Launch" was rebuilt to the designer's layout: the checklist rows now sit directly on the light panel (the white boxes around each row are gone), the grey lines vary in length row to row like real text, and the red "Published" rocket pill now sits at the bottom-left under the list instead of top-right. The completion animation (grey category icons ticking over to red checkmarks, pill going grey to red) is unchanged and still holds on the finished state under reduced motion. All in `components/vignettes/ValueVignettes.tsx` and `app/globals.css`.
- 2026-07-15: Polished the three "How It Works" pictures after designer notes. (1) The rocket in the "See It Before You Commit" browser was redrawn using the designer's exact rocket so it no longer looks broken. (2) That browser's red divider line now sweeps the full width of the window, edge to edge, instead of stopping partway. (3) The "Our Team Becomes Your Team" org chart was made a touch smaller and pulled up so the bottom row of team photos no longer gets cut off, and all three pictures now sit in boxes of the same height. (4) In the "Book an Application Call" video-call grid, the "who's speaking" highlight no longer just flips between one person and the company — it now roams around several team members and the company in turn, like a real call, and the active person gets a soft red ring that grows and fades around their photo like a sound wave. (Team headshots are still the current placeholders; new ones will be swapped in when supplied.)
- 2026-07-15: Replaced the picture in the "Stakes" section ("A great product with a weak brand pays for it every day.") with the designer's new illustration and a looping story in three beats. It shows two stacked product cards — "Your Product" priced €€ and a "Competitor" priced €, both drawn with the same industrial valve line-art so they look identical — plus a tall red "Sales" bar on the right. In the loop: a cursor hesitates between the two cards and clicks the cheaper competitor (it gets a plain grey flash, never red, and a "Picked on price" tag appears) while the Sales bar drains and holds low so the loss lands; then Your Product upgrades its branding — the valve, price, a star badge, the button and a glowing border all turn Nexubis red; then the cursor comes back and clicks Your Product despite the higher price, and the Sales bar fills back up past where it started before the loop repeats. Red only ever means good here (our upgrade and the Sales bar), never the bad click. It lives in `components/vignettes/StakesScene.tsx` with all its styling and timing in `app/globals.css` — the whole loop's speed is one value (`--vs-loop`) and the phase table above the `.stakes-scene` styles maps every step, so the designer can retime it in one place. For anyone who prefers reduced motion the picture holds still on the finished "your brand wins" state.
- 2026-07-15: Rebuilt the three little pictures in the "How It Works" section to match the designer's new mockups, each with its own looping animation. Step 1 ("Book an Application Call") is now a video-call grid, like a Google Meet room: your team and the client each in their own tile with a round photo, and the "who's speaking" red glow flips from your side to the client's and back, with little reaction icons (a thumbs-up, a raised hand) popping up in the corners. The client's tile shows a plain grey building icon so it's clear which one is them. Step 2 ("See It Before You Commit") is a browser window that shows the "before and after": a dull grey wireframe on the left slides into the finished, on-brand version on the right (pink background, red buttons, a red rocket) as a red divider line sweeps across. Step 3 ("Our Team Becomes Your Team") is an org chart that draws itself outward from the red Nexubis mark in the middle — up to the client (the grey building) and down to five team members' photos on red circles, all connected by red lines. The words on the page did not change; only these illustrations did. They live in `components/vignettes/PlanVignettes.tsx` with their styling and motion in `app/globals.css`, and every animation holds still on its finished state for anyone who prefers reduced motion.
- 2026-07-15: Tightened the three "Value stack" pictures to match the designer's mockups more closely and reworked their animations. Tile 1 ("Buyers See Your Worth"): the plain "Built Without Strategy" card now has the bigger, emptier square thumbnail from the design, so it reads as the generic option, and the "Built By nexubis" card lifts more deliberately to win. Tile 2 ("More Output, Faster Turnaround") was rebuilt to the behaviour the designer described: a card lifts off the small left pile, ferries across (past a faint arrow) and lands on the growing fanned pile on the right; the little progress ring in the corner fills across exactly one trip and resets the moment the card lands, then the next card follows — looping forever. Tile 3 ("Brand Ready for Every Launch") is unchanged in layout; its checklist still completes into red checkmarks as the "Published" pill goes from grey to red. All motion still pauses under reduced-motion settings, showing the finished state.
- 2026-07-14: Rebuilt the three little pictures inside the red "Value stack" section (Buyers See Your Worth / More Output, Faster Turnaround / Brand Ready for Every Launch) to match the designer's new mockups, and gave each a gentle looping "before → after" animation. Tile 1 shows a "Built By nexubis" card — pink thumbnail with the red Nexubis mark, a red star bookmark, a red button — that lifts and wins over a flat grey "Built Without Strategy" card. Tile 2 shows a single brief multiplying into a fanned stack of finished pieces, with a small timer ring filling to signal fast turnaround. Tile 3 shows a checklist whose grey category icons (globe, edit, package, calendar) tick over into red checkmarks while a "Published" pill turns from grey to red. The words on the page did not change; only these illustrations did. They live in `components/vignettes/ValueVignettes.tsx` with their styling and motion in `app/globals.css`. The loops are decorative and hold still (showing the finished state) for anyone who prefers reduced motion.
- 2026-07-08: Rebuilt the three "one team / long-term partnerships / we speak industrial" proof cards from small cramped icon tiles into bigger animated diagrams, following the designer's illustrations. The cards sit beside the "We know how it feels..." quote (quote on the left, the three cards stacked on the right), and inside each card the diagram sits on the left with the sentence on its right (on phones the diagram sits above the sentence). The three diagrams (all using the designer's filled icons on dark charcoal circles): (1) six channel icons (code, website, design, 3D, brand, planning) that ride out from behind the red central "team" hub along the connecting lines, growing small-to-big, hold, then shrink and fade back inward before repeating; (2) a rising staircase of charcoal "done" checkmarks with red "+" tops — both the horizontal and the diagonal growth arrows draw out of the bottom-left corner, and the circles scale up as the arrows fill past them, then everything fades and repeats; (3) the designer's full production loop — a funnel (with a red bead that drops quickly onto the first station to start each pass), a roller conveyor with a packaged-box output, and the five stage nodes (bag, quality check, warehouse, truck, box) wired together by a red track; the current eases into view at the first station and flows around the track. Every node sits in the same dark charcoal shade and snaps bright red the instant the current touches it, then eases back to charcoal once it passes (bag → check → box → truck → warehouse); the packaged box also swells a touch as the current runs through it, then it all loops. The animations run at a calm ~6.5–7s pace and all pause under reduced-motion settings, showing the finished diagram instead. The "We know how it feels..." quote also inks in a little lower on scroll now, so the fill is more visible as you reach it.
- 2026-07-08: Fixed the check rejecting a website typed with a trailing dot (for example "dmnwestinghouse.com.", which browsers and copy-paste sometimes add). The address is now cleaned automatically before it is checked, on both the page and the server, so it no longer shows "That website address does not look right."
- 2026-07-08: Full-report (the unlocked report at its shared link) polish. The plain dark bar at the top is gone, replaced by a sticky, branded navigation that stays visible as you scroll: the Nexubis logo on the left, and on the right a "Book an application call" button plus a "Share with your team" button. Share opens a fresh email (Gmail, or your mail app) with the subject, a short summary and the report link already filled in, so you just add who to send it to and hit send. The "Recommended next step" section now uses the same three animated cards as the homepage "How It Works" section. The "first place to fix" section is now on a Nexubis-red background instead of black. And the closing line is bigger, with its two sentences each on their own line.
- 2026-07-08: Fixed a bug where filling in the unlock form with browser autofill showed "Something went wrong with the form." The form has an invisible anti-spam field that only bots should fill; it was named "company", so Chrome's autofill was filling it too and blocking real people. Renamed it so autofill leaves it alone, and told password managers to skip it. Also removed the "(you)" label from the score chips (the red outline already shows which one is yours).
- 2026-07-08: Third Scorecard polish round. On the cover, the two title lines sit closer together. In "First impression", the client's own screenshot is now a large hero shot on its own, with the three competitor screenshots in a smaller, muted row beneath it. The benchmark radar is bigger again, and the competitor lines on it are lighter so the client's red shape stands out more. The "Unlock full report" button in the sticky bar now has a gentle pulsing ring and a soft scale breath to draw the eye (both off under reduced-motion settings), and it no longer loses its button depth while animating.
- 2026-07-08: Second Scorecard polish round from live testing. The report cover now shows the company name in white, bolder and much bigger than the title line (scales down cleanly on phones). In "First impression", the client's own screenshot is unmistakable: red frame, warmer shadow, slightly larger than the competitor shots. In the score section, the overall-score chips went to two lines (name over score), which frees the middle column so the benchmark radar is now much bigger. The unlock form no longer sits at the bottom of the teaser page: the sticky bottom bar is the single unlock surface, its button (and every locked-section "unlock" link) opens the form in a popup, and the button has a soft breathing glow to draw the eye (off under reduced-motion settings).
- 2026-07-08: Scorecard polish round from the first real test run (DMN-Westinghouse), plus three engine fixes. The flow: the website field now shows a green check and border the moment the address looks right; the scan card feels alive (a light sweep travels along the progress bar, and a rotating line tells you what the scan is doing right now, including one personal beat where it names what your site says you make, plus two labelled dad jokes). The teaser: the radar chart no longer clips its category labels and its grid rings are clearly visible; the score dial now sits above a small 0-100 scale showing where the score lands across the three bands (wide 0-59, visible 60-79, narrow 80-100), so "Visible gap" means something; the score chips got a caption explaining who is who; every locked section carries a quiet "unlock the full report" link; and a sticky bar at the bottom of the screen keeps the "Unlock full report" button one tap away on desktop and mobile - it opens the form in a popup, and hides itself when the inline form is on screen. The report cover is now Nexubis red with white type, the company name in ink, and the title flows as one sentence. Engine fixes: (1) reports now address the company by the name written on its own site ("DMN-Westinghouse", not "Dmnwestinghouse"); (2) when the site alone doesn't reveal competitors (niche industries), the tool now finds them through live web search and validates each one - the DMN test now benchmarks against ACS Valves, Roto-Disc and Gericke; (3) an audience gate: sites that are clearly not industrial manufacturers (news sites, shops, agencies) are politely declined before any scanning cost is spent, and anything uncertain still goes through so a real prospect is never locked out.
- 2026-07-08: Made the Scorecard sharper for the industrial buyers we serve, ahead of the first official pass. (1) The auto-detect step now also builds an "industry fingerprint" from the prospect's own site: the industries they serve, certifications they name (like ATEX or EHEDG), their product families, how they sell, and who their likely buyer is. The report copy uses these exact terms, so findings read like they came from someone who knows the industry. The fingerprint is strictly copied from the site, never guessed. (2) The crawler now reads hard facts straight from the pages, with no AI involved: which languages the site offers, the footer copyright year, links to datasheets/CAD files/product selectors, and how many fields the enquiry form actually asks. (3) Two checks were upgraded: "Consistency with LinkedIn" became "Language coverage for export markets", and "Jargon level" became "Path to a quote" (does the enquiry form take a serious buyer seriously?). These are the gaps that actually cost industrial manufacturers sales. (4) The report can now name concrete competitor advantages measured on their sites ("they offer 9 languages; your site offers one") - computed in code from the same measurements on both sides, so it can never be invented. (5) One extra web search checks whether the prospect even shows up when a buyer searches for what they make. Cost impact: about 1 to 3 cents more per fresh run. Every new signal is built so absence of proof is reported as "could not be checked", never as a false accusation.
- 2026-07-08: Reworked the Scorecard entry page. (1) It now uses the same header as the homepage instead of the old dark bar, and there is proper breathing room between the header and the page content (a spacing value that had been silently missing site-wide is now set, so the same gap appears anywhere it was meant to). (2) The form now asks for **only the website**. What the company makes and who their competitors are is worked out automatically from their site, so there is far less to fill in. (3) The entry card was redesigned in the split "hero + card" style, with a preview chart of the five areas we score (Brand, Website, Visuals, Print, Message) sitting above the website box. (4) The headline "How Credible Is Your Brand, Really?" is now Title Case. Behind the scenes: a new auto-detect step reads the site, infers the one-line description and up to three competitors, and the existing engine benchmarks against them; results are cached per website so re-runs stay free and identical.
- 2026-07-08: A round of homepage polish. The hero's "Check your brand's credibility" link is now a proper outline button sitting beside the red one, and the "Not ready to book?" line is smaller fine print. The big headline now flows as one sentence instead of breaking oddly, and every headline on the homepage uses Title Case ("How It Works", "Buyers See Your Worth", and so on). The three outcome tiles now sit on a bold Nexubis-red section background. The big "We know how it feels..." statement greys out and inks in word by word as you scroll, and the Nexubis logo above "We've become the in-house creative team..." was removed. The two busier guide icons (team hub, growth chart) were simplified and flattened to match the conveyor icon's clean style.
- 2026-07-07: Two more visual upgrades in the new soft "glass" style. (1) The three small icon tiles in the "one team / long-term partnerships / we speak industrial" cards are now little glass dioramas: a glowing red team hub with five channel bubbles gently bobbing around it, a rising bar chart whose red growth arrow redraws itself on a loop, and a hopper that pours a stream of red granules onto a conveyor. (2) The dark "A great product with a weak brand pays for it every day" section now has glassy product cards with a sweeping light reflection, a "Your product" glass pill on the first card, and a loop where a cursor glides in and picks the cheaper lookalike, making its red glow bloom. Also fixed a small bug where the "Picked on price" pill had stopped appearing. All motion pauses under reduced-motion settings.
- 2026-07-07: Redesigned the three "How it works" step illustrations (Book an application call / See it before you commit / Our team becomes your team) in the same soft, dimensional style as the outcome tiles. Step 1 now shows the team joined to a red calendar by a flowing dashed arrow, with a cursor that glides in and presses the Book a call button on a loop. Step 2 is a browser with a before/after slider that sweeps back and forth, revealing the branded design over the gray wireframe. Step 3 is a channel list (website, brand, 3D, video, print) where a red highlight walks down the rows one by one, pulsing each checkmark as it passes. All loops pause for visitors who prefer reduced motion. No wording changed.
- 2026-07-07: Redesigned the three outcome-tile illustrations near the top of the homepage (Buyers see your worth / More output, faster turnaround / Brand ready for every launch) to a softer, more dimensional style: floating white cards with deep soft shadows on a subtle blush-tinted panel, matching new reference art. Each one now plays a smooth repeating animation: a cursor glides over and picks the branded product card, a brief card flies across into a growing stack of deliverables while a gauge sweeps, and a checklist ticks itself off before a "Published" button pops in with a light shine. The animations pause automatically for visitors who prefer reduced motion. The words on the tiles did not change.
- 2026-07-07: Built the complete Brand Credibility Scorecard tool (the Section 7 lead generator). New: the `/scorecard` check flow, shareable reports, the team admin, lead capture with Funnelr webhook and team notifications, and the full AI scoring engine behind it. Three real manufacturer sites were scored end to end to tune the output. The homepage entry points now resolve to the working tool when the Scorecard switch is on.
- 2026-07-07: Elevated every homepage section with hand-built illustrations ("vignettes") in the old process-card style — small, realistic fake interfaces drawn in code (no stock images, no icon libraries). The words on the page did not change at all; only the visuals did. New vignettes live in `components/vignettes/`. Each one is decorative and animates gently as you scroll (and holds still if you prefer reduced motion). Two spots wait on real footage and show a tidy placeholder until it arrives: the Oxipack video testimonial (`oxipack-testimonial.mp4`) and the trade-show booth loop behind the closing statement (`booth-loop.mp4`).
- 2026-07-06: Rebuilt the homepage to the locked 12-section wireframe (new industrial-manufacturer positioning and copy). Added `lib/site-config.ts` so every CTA reads from one place, with `BOOKING_URL` pointing at contact for now. Added the Scorecard feature switch. Updated the header CTA and the footer links, social, and sign-off. Retired the old testimonial wall, the old process cards, the old solutions grid, and the "Empowering Dreams" closer from the homepage. The relocated components (`WorkSection`, `TestimonialsCarousel`) remain in the repo for reuse on other pages.
