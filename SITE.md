# Nexubis

> Look as credible as your engineering, with one in-house creative team.

Nexubis is the in-house creative team for European industrial manufacturers whose product is better than their brand shows. This site sells that positioning: brand, website, 3D, video, and print on one flat monthly retainer.

## Brand Identity

- Personality: confident, industrial, premium, plain-spoken. Sells on outcomes, not adjectives.
- Colours (existing tokens, unchanged): red accent `#FF4141`, near-black `#1d1c1a`, body grey `#5a5a5a`, off-white surfaces `#f2f2f2`, deep "work black" `#0f0f0f` for the dramatic dark sections.
- Fonts (existing, unchanged): Helvetica Now Display for headings and confident lines, Inter for body.
- Motion: subtle. A hero intro sequence plus a gentle "lift and fade" as each section scrolls in. Everything respects reduced-motion.

## Pages

- **Homepage** (`/`) - The locked 12-section story. See below.
- **Packages** (`/packages`) - Pricing page, rebuilt. Three tiers (Momentum, Scale, Partner) with a currency toggle (EUR/USD) and a billing toggle (Monthly/Quarterly/Annually, Annually pre-selected). Only Partner has prepayment discounts (shown in a green "Save €X" pill); Momentum and Scale show "No Discount Applied". Every price, feature and FAQ answer comes from `lib/packages.ts` and `lib/packages-faq.ts` - change a price or a feature in one line there. Hover the "i" on any feature for detail. Full write-up in `docs/architecture/PACKAGES_V2.md`.
- **The Online Credibility Audit** (`/audit`) - the instant lead-generator tool (renamed from the Industrial Brand Credibility Scorecard on 2026-09-01; every old `/scorecard` link redirects here permanently). A visitor enters **their website and work email** up front - nothing else. The tool reads their site, works out what they make and who their competitors are on its own, checks all of them (crawl, first-impression screenshots, loading speed, web presence), scores 25 checks with Nexubis AI, and shows a preview on the spot. The page uses the same header as the homepage. The hero leads with "Your Brand, Scored out of 100." and a white kicker naming the audience ("For European industrial manufacturers"). On desktop it is two columns: the heading, subhead and bullets on the left, and a card on the right with an animated preview of the five areas we look at (Brand, Website, Visuals, Print, Message) on top and the single website field ("Enter your company website") plus the "Show my score" button below it. On mobile the field and button sit directly under the heading, with the preview below them. There is no unlock step: the moment the check finishes, the visitor lands on their full report, and a copy is emailed to the address they gave.
- **Shared reports** (`/audit/r/...`, old `/scorecard/r/...` links redirect forever) - Each unlocked report lives at its own private link for 180 days: score, competitor benchmark, five category pages, the first place to fix, the Oxipack proof, and the booking step. Not indexed by search engines. A personal video can be attached from the admin and appears at the top.
- **Audit admin** (`/audit/admin`) - Password-protected team area: the leads table (with a "Loom candidates" view for the weekly video selection), notes, and per-report tools (attach the video, regenerate, copy the link). Not indexed.

## Homepage sections (in order)

1. **Hero** - Headline, sub-headline, the "Book an application call" button, the showreel video, and the client logo strip.
2. **Value stack** - Three outcome tiles: Buyers see your worth / More output, faster turnaround / Brand ready for every launch.
3. **Stakes** - A short dark narrative about what a weak brand costs a great product.
4. **The Guide** - An empathy line, our authority line, and three proof bullets. No client names or stats here.
5. **Solutions** ("What we take off your plate") - Five services in an accordion, each with a "Learn more" link: Brand identity, Website, 3D & CGI, Video & motion, Trade show & print.
6. **The Plan** ("How it works") - Three numbered steps, a summary paragraph, and the booking button.
7. **Online Credibility Audit** - The lead-generator block. Hidden by default; see "The audit switch" below.
8. **Proof** - One customer quote (DMN, currently a clearly marked placeholder to collect) and one case tile (Oxipack) linking to the case study.
9. **Success statement** - A vivid "picture the next trade show" paragraph and the booking button, set over a scroll-scrubbed trade-show video (see "The success video" below).
10. **Proof bar** - A slim band with three short promises.
11. **FAQ** - Seven questions in an accordion.
12. **Footer** - Links, social, and the sign-off "Built brilliantly. Branded to match."

## Components

- **Header** (`components/SiteHeader.tsx`) - Logo, navigation, and the "Book an application call" button. The audit is not linked from the header; when it is switched on, the Credibility Check appears in exactly three places: the hero transitional link, Section 7, and the footer.
- **Footer** (`components/SiteFooter.tsx`) - Wordmark, link menu (About, What we do, Work / Case studies, Online Credibility Audit when on, Contact, Privacy & terms), social (LinkedIn, Instagram), and the sign-off line.
- **RevealOnScroll** (`components/RevealOnScroll.tsx`) - Drives the gentle scroll-in motion for the new sections.
- **HeroAnimations / FooterAnimations** - Existing motion for the hero and footer, kept as-is.

## The one place to change links: `lib/site-config.ts`

Every button and important link on the homepage reads from this one file, so you never hunt through the code:

- `BOOKING_URL` - Where "Book an application call" goes. **Now the real calendar: `cal.com/nexubis/30min`** (sourced from `lib/booking.ts`, the single place any cal.com link lives). Report pages use a smarter version that also carries the person's name, business, **their website**, and report link into the booking form (matching the live Cal.com field identifiers: `name`, `title`, `Website-Link`, `Report-Link`). The email is deliberately NOT prefilled — the report link is shareable/public, and the visitor's email is kept server-side, so putting it in the booking link would leak it.
- `SCORECARD_URL` - The Online Credibility Audit tool (`/audit`). The constant keeps its old code name; only the address and the visitor-facing name changed.
- `CONTACT_URL` - The contact page.
- `OXIPACK_CASE_URL` - The Oxipack case study link.
- `FOOTER_LINKS` / `SOCIAL_LINKS` - The footer menus.

## The audit switch (a feature flag)

Everything about the Online Credibility Audit is hidden until you turn it on. When on, it appears in exactly three places: Section 7, the hero's "Check your brand's credibility" link and support line, and the footer link. It is deliberately not in the header, so it never competes with the "Book an application call" button.

- **To turn it on:** set an environment variable `NEXT_PUBLIC_SHOW_SCORECARD` to `true`, then rebuild.
- **When off (the default):** the whole Section 7 block is completely absent (no empty gap), and the hero shows only the "Book an application call" button, laid out to look intentional that way.

## How to customise

- **Change any CTA destination:** edit `lib/site-config.ts`.
- **Change wording:** the homepage copy lives near the top of `app/page.tsx` as clearly named blocks (hero, value tiles, stakes, guide, solutions, plan, scorecard, proof, success, proof bar, FAQ).
- **Change colours or fonts:** edit the tokens at the top of `app/globals.css`. The whole site follows them.
- **Turn the audit on or off:** the `NEXT_PUBLIC_SHOW_SCORECARD` switch above.

## The Online Credibility Audit tool (how it works, in plain words)

- **Where things live:** the engine is in `lib/scorecard/` (crawling, screenshots, scoring, emails, storage), the screens in `components/scorecard/`, and the pages under `app/audit/`. (The code folders keep the old `scorecard` name on purpose; only the web addresses and visitor-facing words changed.) All the words a visitor sees are in one file, `lib/scorecard/copy.ts`.
- **What a run costs:** roughly 20 to 30 US cents in API fees per fresh check (measured, logged per run). Repeat checks of the same company are free thanks to caching.
- **Leads:** every unlock saves a lead (visible in the admin), pings the team by email, and sends the contact to Funnelr by webhook so the email sequence starts. If Funnelr is not connected yet, a built-in first email can be switched on with `SCORECARD_SEND_EMAIL1=true`.
- **House rules are enforced by tests:** no em dashes anywhere, Oxipack is named exactly once per report, and the AI only states what it actually saw. (The old rule banning the word "audit" was retired with the 2026-09-01 rename; the tool is now literally called an audit.)
- **Test drive without spending:** `SCORECARD_MOCK=1` makes the whole tool run on canned data (currently ON in local development). In production this must be OFF.
- **Before launch:** work through `docs/qa/LAUNCH_CHECKLIST.md` (new database, email domain, bot-check keys, Funnelr webhook, privacy wording).

## Recent Changes

- 2026-09-02: **Pre-launch landing tweaks.** (a) Restored the five-pillar radar preview (`ScorecardPreviewRadar`) in the audit form card, replacing the "Video coming soon" poster placeholder. Laine's intro video still takes this slot automatically once `LAINE_VIDEO_SRC` is set in `laine-video-config.ts` — so the campaign can go live now and the video drops in later with no layout change. (b) Title Case pass to match the Nexubis headline convention: the landing hero headline ("The Buyer Who Didn't Trust Your Website Shortlisted Your Competitor.") and all audit CTA buttons — "Run My Free Audit", "Unlock Your Free Session", "Unlock Your Session", "See Your Previous Report", "Try Again", "Check a Different Address" (minor words like "a" stay lowercase). Site header buttons ("Get Free Audit", "Book a Call") were already Title Case. Acceptance test updated for the new button string.
- 2026-09-02: **Report polish pass — spacing + shorter copy.** Six tweaks to the report: (1) the "Where the competitors stand" card is now centre-aligned (title, radar, score chips), scoped so the landing-page competitor picker is untouched; (2) the red score-hero band now has equal, tighter red space above and below the white card (removed the extra top margin); (3) Laine's sidebar card lead is one short sentence; (4) the gap between the sidebar "On this page" card and Laine's card is reduced; (5) the dark "Your next step" offer block copy is tightened (lead, both claim cards, risk-reversal). **All of the above apply to every report immediately** (they're layout + fixed copy). (6) The AI-written "Where we would start" section (fix list, "why it comes first", "what fixing it looks like", and the cost-of-doing-nothing line) is now generated much shorter — word budgets lowered in the generation prompt AND the hard clamp table (`lib/scorecard/clamp.ts`): start items 11 words / max 4, staying-same 20, first-fix why 26, in-practice 30. **This one only affects NEW audits**, because a report's AI copy is trimmed and stored at generation time, not re-trimmed on view. Acceptance tests pass; the demo report (`/audit/r/demo`) shows 1–5 but keeps its old-length section 6 because it was generated before the change. Follow-ups: widened the sidebar rail (300px → 330px) and trimmed Laine's card side padding so its copy stretches further horizontally; added top spacing on the offer block's scarcity line so it sits lower under the "Unlock your free session" button.
- 2026-09-02: **Booking form prefill widened to the website field.** The report's booking links now also prefill the audited **website** (Cal.com field `Website-Link`), on top of the name, business (`title`) and report link that were already carried. Wired the audited URL into the report data (`ResultMeta.websiteUrl`, set from the prospect's normalised site) and threaded it through every report CTA (offer block, top nav, Laine sidebar card, mobile bar). Confirmed the live Cal.com field identifiers against the event's public API before wiring. Email is intentionally left un-prefilled (the report link is shareable and the email is server-side only). Note for Laine's separate Cal.com dashboard edits (rename event, remove "Package" and "Additional Notes"): those are event settings, not code — safe as long as the event **URL slug** stays `30min`; if the slug changes, set `NEXT_PUBLIC_CAL_EVENT_SLUG` (or `NEXT_PUBLIC_CAL_BOOKING_URL`) to match.
- 2026-09-01: **Post-restructure cleanup.** Audited every external service against what the reworked pipeline actually calls: all of them are still in use (details in the session notes), so nothing cancels outright; ScreenshotOne is the one downgrade candidate (renders per audit halved, and reports no longer serve images from its CDN). Removed as dead: the legacy Funnelr signed-webhook module and its config (the tag bridge replaced it long ago), the unused report-hero screenshot helper, the unlock-era form field copy, the unused `resend` npm package (we call their API directly), and 84 dead CSS rules from the unlock gate, teaser, screenshot exhibits and the old report layout. Also restored something the layout rebuild had silently dropped: the first fix's "why it comes first" and "what fixing it looks like" paragraphs now render inside the "Where we would start" card, which the landing page promises.
- 2026-09-01: **Full pre-launch regression pass.** Real end-to-end audit (kudernak.de, test email): form to staged narration to full report, Funnelr enrolled with all three tags, internal alert and the visitor email sent. Old /scorecard URLs redirect permanently. Every layout-spec section verified present on the real Kudernak report by DOM assertion and 9 mobile screenshots at 375px. Tracking verified: AuditStart and Lead fire together at submit with distinct event ids, email on the server leg, identity cookies minted; server events carry the test event code for the Events Manager check. Zero visible "Scorecard" across every rendered page. Deferred to post-deploy: Lighthouse (production build needs Sanity credentials not present locally) and the Events Manager visual confirmation. Watch item: the deck-copy AI pass occasionally times out under load; the templated fallback rendered a complete report when it did (by design), but generated copy is richer, so watch the "templated fallback used" flag in production scans.
- 2026-09-01: **Rewired Meta tracking for the gateless flow.** AuditStart (the campaign optimisation event) and Lead now BOTH fire at the successful URL+email form submit, as distinct events so Events Manager keeps the funnel split; both fire on the browser pixel and the server Conversions API with one shared event id for deduplication, and the raw email rides the server leg only (hashed there) together with the self-minted _fbc/_fbp/external id. New **ReportViewed** event fires when a generated report renders (once per page load; the demo report is excluded). AuditComplete stays diagnosis-only at scan finish; AuditBookClick (CTA click) and Schedule (confirmed cal.com booking, server-side) verified unchanged. Still to do by hand in Ads Manager: switch the campaign optimisation event to AuditStart and confirm it was never set to the book-a-call event.
- 2026-09-02: **Laine sidebar card condensed + product-wide language/tracking/booking pass.** The Laine card now mirrors the offer in miniature (photo/name/role kept): a two-sentence lead, three red-checked points (thirty minutes live / homepage reworked before the call / yours to keep, fit or no fit) and the CTA, kept compact. Language sweep across landing + report + emails: standardized the score name to "Gap Score" (fixed report scoreTitle, the score-ring aria-label, the report meta description, the internal lead-alert email), removed the last "the competitors you named" implication (report meta + a landing card now use "the closest competitors showing up online" / "buyers weigh you against"), and standardized the CTA to "Unlock your free session" everywhere (offer button, Laine card; compact chrome uses "Unlock your session"). No visible "Scorecard", "Loom", "book a call", "application call", "Credibility Score" or "score out of 100" anywhere in the audit product; the only remaining occurrences are internal identifiers, tracking params (content_category, LEAD_CONTENT_NAME) and the admin walkthrough-video feature, all deliberate. Tracking: the offer CTA now fires **SessionUnlockClick** (canonical) AND the legacy **AuditBookClick** for one campaign cycle (both names noted in code); **AuditStart is unchanged** and confirmed firing on the new three-field form submit. Booking: the Cal.com target is env-configurable (NEXT_PUBLIC_CAL_BOOKING_URL, or username/slug) via lib/booking.ts, used by both the offer block and Laine card. The 2-working-day minimum notice is NOT in code (documented): it must be set on the Cal.com event type (Limits -> Minimum Notice); Cal.com's minimum notice is a flat duration, so pair ~2 days with Mon-Fri availability. QA: real end-to-end run on jasa.nl completed in ~114s, unlocked instantly, first name carried into the Funnelr payload (real API ok:true; nameless payload rejected), Gap Score naming consistent, offer + Laine correct desktop and mobile, zero console errors, confirmation email fired with the first name merged.
- 2026-09-02: **Report offer block rewritten (unlock a session).** The dark card at the report close now leads with an eyebrow "YOUR NEXT STEP", the heading "Unlock a live session on your audit, with your homepage already reworked.", the new intro, the two existing-style cards ("Your homepage, reworked before we meet." / "Thirty minutes, live, nothing for sale."), a visually distinct risk-reversal panel ("You keep everything either way..."), a quiet scarcity line ("We run five of these sessions a week."), the unchanged stakes line with the competitor merge field, the button "Unlock my free session", and the under-button line. Same container, position and card styling; copy/structure only. Hard rules enforced across the WHOLE report: the word "Loom" no longer appears anywhere visible (the walkthrough-video feature stays, its label never said "Loom"), and "book a call" / "application call" / "sales call" are gone from every surface, the offer button, the sticky top-nav button, the sidebar Laine card, the mobile bottom bar and the on-this-page nav label all now say "unlock a session". No pricing or cost numbers. The booking link and its AuditBookClick tracking are unchanged, only the labels. Removed the dead (unrendered) "Recommended next step" copy that still carried "application call"; acceptance test updated to guard the new offer copy.
- 2026-09-02: **Audit landing: first name field + copy sweep.** The form now asks for company website, then First name (placeholder "Your first name"), then work email, all three required; the email small print is unchanged. The first name flows the whole way: it is validated server-side (an empty first name is rejected at capture, reason "first-name"), stored on the lead, carried in the payload posted to the Funnelr bridge (as both firstName and an explicit first_name alias, so the merge field every sequence email uses is always populated), and used in the confirmation email. The bridge logs a clear error if a payload ever arrives without a first name. Copy consistency on the page: the score is "Gap Score" (fixed the What-You-Get card and the scan step), the timing claim is "about 2 minutes" (fixed the scan note), no wording implies the user names their own competitors (fixed the confirmation email), and "Scorecard" appears nowhere visible. Also fixed a spacing bug where the video slot overlapped the first field label on desktop (the video box zeroes the radar's negative bottom margin). Hero headline, subhead, bullets, pillar chips, What-you-get cards, Oxipack proof and the video placeholder were left untouched.
- 2026-09-02: **Navigation + CTA labels.** The book CTA is now "Book a Call" (was "Book an application call") and the audit CTA is "Get Free Audit" (was "Check your brand's credibility") everywhere on the homepage and header. Desktop header now carries BOTH as buttons on the right: "Get Free Audit" (outline) beside "Book a Call" (red). On mobile the header bar is just the logo and the hamburger, no CTA; the two CTAs moved into the hamburger dropdown below Case Studies, "Book a Call" as the primary (red) and "Get Free Audit" as the secondary (outline), full width. The audit CTA is gated on the same NEXT_PUBLIC_SHOW_SCORECARD flag as the rest of the audit surfaces.
- 2026-09-02: **Report + landing design pass (design review).** Report: removed the industry framing line under the company name; moved the AI disclaimer up to sit directly under "POWERED BY NEXUBIS AI" in the hero (light italic on the red); centred the Gap score ring over its band strip (Tailwind's preflight makes the SVG display:block, so it needed margin-inline:auto, not text-align); removed the "Recommended next step" 01/02/03 block and the "You've done good work to get here" closer (kept a slim contact footer). Landing: the form-card visual is now the Laine intro-video slot instead of the radar preview (a poster + "Video coming soon" placeholder until the clip lands; set `LAINE_VIDEO_SRC` and the real 16:9 video drops straight in), and the redundant below-fold video section is gone. Deleted the now-unused LaineIntroVideo component; ScorecardPreviewRadar is retired from the landing.
- 2026-09-01: **Rewrote the audit landing page copy** around one job: run the free audit. New headline "The buyer who didn't trust your website shortlisted your competitor." with the procurement-team subhead, three value bullets, the five pillar chips (Brand, Website, Visuals, Print, Message), and the two-field form now submitting with **"Run my free audit"** and "Free · Your full audit in about 2 minutes, unlocked instantly" underneath. Below the fold, three short additions: a "What you get" strip (your Gap score out of 100, the named-competitor comparison, the first thing to fix), one Oxipack Netherlands proof line linking to the case study, and a **video slot for Laine's intro clip** (16:9, poster frame, showing a "Video coming soon" state below the fold for now; when Leon's script is shot, set `LAINE_VIDEO_SRC` in `components/scorecard/flow/laine-video-config.ts` and the clip REPLACES the radar preview at the top of the form card while the below-fold placeholder disappears, all from that one switch). Checked at phone width: the mobile hero order (eyebrow, headline, form, subhead, bullets, chips) holds and the reassurance line wraps cleanly.
- 2026-09-01: **Hardened the audit pipeline against the LekkeWeb dossier.** Compared every reliability mechanism point by point (the full comparison and the combined rules live in `docs/audit-hardening.md`). Already equal or stronger here: URL cleaning and the address blocklist, the never-hang timeouts, the "a failed measurement never lowers a score" rule (ours excludes unassessable checks at two levels), duplicate handling, and content safety. Newly added from the dossier: the web address is fully checked with a specific friendly reason before anything runs; an email domain that cannot receive mail is caught before the scan (typo protection, forgiving of network wobbles); the whole generation races a 4-minute deadline so a hang is impossible; a report scored on fewer than 3 of 5 pillars is not shipped; **any failure after the email is given keeps the lead** (the team is emailed to run the audit by hand, and the visitor is told exactly that); the rate-limit bounce now shows the visitor their previous report link; and the capacity breaker emails the team at most once per hour. Tested against hostile inputs: no-protocol URLs, dead domains, redirects, password walls, 10-second-slow sites, JS shells, dead-email domains, bot traps and double submissions all end in either a report or a clear friendly message.
- 2026-09-01: **The generator now writes every section the new layout shows.** The AI copy pass produces: a one-line header verdict naming the biggest leak; per-pillar "What is working" and "What to fix" lists (short bold title plus one sentence each, every fix hinting at the solution); the "Top 3 things costing you customers" with impact estimates stated as defensible ranges ("Likely 30-50% of non-German visitors lost before contact"), never invented precision; the prioritised "Where we would start" list written as outcomes plus the "what staying the same costs you" sentence. The issues count is computed from the fix lists, and the top competitor keeps flowing into the offer block. Word caps and the content-safety scan cover every new block; if generation fails, the fallback builds the same sections from the measured check scores, so no report ever misses them. Reports stored before this change keep rendering via the same fallback. **Netherlands emphasis:** Dutch companies (.nl or Dutch-language sites) now get credible Dutch competitors preferred in the comparison set. Verified with a real regeneration of the Kudernak report: every new field rendered, grounded in its evidence (datasheets missing, German-only site, Trelleborg as the named comparison). Everything the generator already produced stays: pillar scores /20, benchmarks, speed table, "What we looked at", first-place-to-fix.
- 2026-09-01: **Wrote the book-a-call offer section** at the bottom of the report. Claim language, not unlock language, no pricing: heading "Book a call to claim your free landing page rework and feedback Loom", a lead-in ("we will rework the top of your homepage before you pay us anything, you judge the work, not the promises"), two claim cards (the free hero rework; the short Loom walking through it), a stakes line naming the top competitor from their own audit ("Skip it and nothing changes..."), the "Book your free call" button, and reassurance small print ("No invoice, no pressure, and you keep the audit and the rework either way."). All the copy lives in one block, `BOOK_CALL` in `lib/scorecard/copy.ts`, for Leon's Grand Slam review. The Laine sidebar card keeps "Book a call and we'll walk your audit together, no obligation."
- 2026-09-01: **Rebuilt the report page layout** to match the structure of the LekkeWeb audit report, in Nexubis fonts and colours. New order: red hero with a framing line ("For a [what they make], here is where [Company] stands online."), the untouched score gauge with the Wide/Visible/Narrow band strip, a one-line verdict, an issues count and clickable pillar chips; an AI disclaimer line; the competitor comparison (chips + five-pillar radar); five pillar cards each with a tinted score badge, a one-line summary, "What is working" and "What to fix" lists (every item a bold title plus one sentence, long lists collapse behind "View more (N)"), the competitors line, the loading-speed table (Website pillar) and a collapsible "What we looked at"; "Top 3 things costing you customers" with impact estimates; "Where we would start with [Company]" plus a "what staying the same costs you" line; the Netherlands Oxipack proof with the showreel; a dark book-a-call card (final copy comes later; no pricing anywhere); the sticky "On this page" sidebar with Laine's card; then the 01/02/03 next-step block and the closer. **On a phone** the sidebar becomes a sticky "Jump to" dropdown under the header, a book-a-call bar stays pinned to the bottom, and Laine's card appears inline near the end. All the new list content is computed from the 25 stored check scores and evidence sentences, so **every already-generated report renders the new layout with no regeneration**. Code: `components/scorecard/report/` (ReportView, CollapsibleList, JumpToNav, MobileCtaBar), `lib/scorecard/report-derive.ts`, styles at the end of `app/globals.css`.
- 2026-09-01: **Removed the unlock gate and made the audit faster.** The flow is now: enter your **website and work email** up front (small print under the email field: "We'll email a copy of your audit to this address."), the check runs, and you land straight on your **full report** - no partial preview, no unlock form, no locked sections. The email behaviours stayed: the report link is emailed (while the built-in first email is switched on) and the Funnelr sequence starts from that address; a best-effort first name is read from the email itself (skipped for generic mailboxes like info@). The bot protections that used to sit on the unlock form (invisible trap field, too-fast check, Cloudflare bot check, disposable-email block) now guard the run itself, so nothing generates before they pass. Running the same site twice with the same email returns the same report link and never emails twice. **The wait is shorter and honest:** the generation pipeline was profiled and reworked - the Google speed measurement no longer holds everything up (it runs alongside the AI scoring), screenshots are lighter (smaller format, competitors captured desktop-only, which also stops their captures failing under the screenshot service's parallel limit), and the two rescue searches run together. A fresh run on the same test site went from **108.6s to 88.2s (~19% faster)**; the remaining time is mostly the three AI passes themselves. The waiting screen now narrates five real stages as they genuinely happen: Reading your website / Measuring loading speed on mobile and desktop / Finding the competitors buyers weigh you against / Scoring your five pillars / Writing your report. Old report links and already-generated reports render unchanged (they were always the full report; only new visitors used to see the locked version). Timing logs (`[scorecard-timing]`) now print per step in the server logs, and `scripts/scorecard-profile.ts` runs a full real profile (~25 cents).
- 2026-09-01: **Renamed the tool: the Industrial Brand Credibility Scorecard is now "The Online Credibility Audit".** The industrial positioning lives in the eyebrow line ("FOR EUROPEAN INDUSTRIAL MANUFACTURERS"), which stays on the landing page; the name itself no longer carries it. The landing page moved from `/scorecard` to `/audit` and reports from `/scorecard/r/...` to `/audit/r/...`; every old `/scorecard` address (report links, QR codes, emails already sent) redirects permanently to the new one, so nothing anyone saved ever breaks (`next.config.ts`). The new name now appears in page titles, social-share cards, the report cover, the footer link, the homepage block, the loading screen and both emails (the team alert now says "Audit lead:", the visitor email "Your Online Credibility Audit is ready"). Nothing about the layout, flow or other wording changed. Deliberately kept under the old name: the code folders (`lib/scorecard/`, `components/scorecard/`), the hidden `/api/scorecard/...` addresses, the stored data keys, the Funnelr tag/field names and the Meta tracking labels, so history, integrations and ad statistics stay unbroken. The old house rule that the word "audit" must never appear for visitors was retired, along with its tests.
- 2026-08-25: Scorecard landing hero rewrite (Copy Brief v1). The ads promise a "score out of 100"; the old page opened with a rhetorical question ("How Credible Is Your Brand, Really?") and never named the audience, so the words on the page did not match the words people clicked. The hero now leads with **"Your Brand, Scored out of 100."** (Title Case), a **white** kicker that names who it is for (**"For European industrial manufacturers"**, sized down on mobile so it fits one line), and a shorter one-line subhead. The website field label is **"Enter your company website"**. The button changed from "Check my brand's credibility" to **"Show my score"** (the outcome, not the process), with a reassurance line under it: **"Free brand scorecard"** in red, then **"Done in under 2 minutes"** in grey. The old scarcity line ("we take on two partners a month…") is gone. **Layout:** desktop is two columns (heading, subhead and bullets on the left; a card on the right with the animated five-point preview on top and the website field + button below). On mobile the field and button sit directly under the heading, with the preview below them. The hero chart stays the **animated five-point preview** (no numbers, no title label, no legend, no box) sitting directly on the card, enlarged, showing what the tool measures. **Speed claim corrected to "under 2 minutes"** everywhere on-site (scorecard subhead, homepage hero support + scorecard block, packages trial, scan loading note, expired-report messages). All copy lives in `lib/scorecard/copy.ts` (`LANDING`); layout in `components/scorecard/flow/ScorecardFlow.tsx` and `app/globals.css`. Event names (AuditStart, Lead, AuditBookClick) were not touched. **Note:** the competitor count is deliberately kept vague ("your closest competitors") because the scan resolves up to three but the number varies. An earlier draft that named the client roster (Oxipack, Altify, Circuit) in a hero proof line and rendered a numbered example scorecard (score 68, sample finding) plus a below-the-fold methodology line was removed at Hannes's request; the hero is deliberately leaner.

- 2026-08-24: Added an automatic low-balance/health monitor for outside tools, then expanded it to cover every paid service the Scorecard leans on. Background: the Scorecard's web-search provider (Serper) quietly ran out of credits, which silently stripped the competitor benchmark out of every report with no warning. Now a scheduled check runs once a day (7am) and **emails hello@nexubis.io** the moment a tool is running low or has stopped responding. Coverage: **Serper credits** and **ScreenshotOne screenshot credits** (both alert before they hit zero, since each has a live balance we can read); **Claude/Anthropic** (validates the key works, and if you set an admin key + a monthly budget it also warns as spend approaches that budget — Anthropic has no "credit balance" to read, only spend); **Google PageSpeed** and **Resend email** keys (alerts if either stops working); and the **Upstash store** that holds every run and lead (alerts if it stops responding). It re-alerts at most once a day per tool so it never spams. Nothing required to configure — sensible defaults are built in; tune the warning levels with `SERPER_LOW_BALANCE` / `SCREENSHOTONE_LOW_BALANCE`, switch on Claude spend alerts with `ANTHROPIC_ADMIN_KEY` + `ANTHROPIC_MONTHLY_BUDGET_USD`, and change the alert address with `MONITOR_ALERT_EMAIL`. Deliberately not alerted (free or degrade gracefully): Turnstile, Funnelr, Sanity, and the JS-render key. Code in `lib/monitoring/dependency-health.ts` + `app/api/cron/dependency-health/route.ts`; schedule in `vercel.json`. Easy to extend to more tools later (one entry per tool).

- 2026-08-24: Scorecard audience gate removed (`remove-audience-gate`, PR #25). A real-phone test found the tool was **wrongly turning away genuine manufacturers** (a Cape Town tool maker, a tile maker) at the "this is built for industrial manufacturers…" screen, while letting a non-manufacturer through. Because every rejected visitor already cost a paid ad click — and the rejection happened after they'd started — this was a hidden third way people "started but never finished," on top of giving up during the wait and refusing at the email gate. **The gate is now gone: no website you enter is ever rejected.** Off-topic sites are still held back only by the abuse limits (one run per person per week, per-company daily caps, an overall ceiling). The tool still quietly notes what it *thinks* each site is (manufacturer, adjacent, outside, unclear) purely as insight — it never blocks on it. That insight, plus every run's outcome and how long it took, now shows in a new **password-protected admin "Scans" page** (`/scorecard/admin/scans`), so you can see exactly which links came in and what happened to each. Trade-off on the record: off-topic submissions now run a full (paid) scan instead of being cheaply stopped early; deliberate, since never losing a real manufacturer is worth more and volume is capped by the limits.
- 2026-08-24: Scorecard tracking + loading experience pass (feat/prelaunch-polish). (1) **Verified the Meta tracking is production-clean:** live events only ever send from `nexubis.io` / `www.nexubis.io` (host gate in `lib/meta/config.ts`), cookies are written on `.nexubis.io`, and the production Vercel env carries neither a test-event code nor a force flag — so real events land in the live dataset, not Test Events. PageView was confirmed to already fire once per genuine page view (not over-firing on the scorecard), so it was left alone. (2) **New diagnosis-only `AuditComplete` event** fires the moment the scan finishes and the teaser/gate renders (`components/scorecard/flow/ScorecardFlow.tsx`), so we can finally tell people who abandoned during the wait (AuditStart − AuditComplete) from people who saw the gate and refused (AuditComplete − Lead). It is deliberately **not** a custom conversion. Mirrored server-side by a per-run outcome + duration log in Upstash (`lib/scorecard/diagnostics.ts`), so a broken scan is distinguishable from an abandoned one. The three live event names (AuditStart, Lead, AuditBookClick) are untouched. (3) **The loading screen no longer tells dad jokes.** It now narrates the real audit stages in the calm Nexubis voice, driven by the actual pipeline (`components/scorecard/flow/ScanAnimation.tsx`, copy in `lib/scorecard/copy.ts`), keeping the determinate progress bar and the four-stage checklist. (4) **The failure state now has a way back in:** a plain-language explanation plus a "Try again" button (replays the same address) and a "Check a different address" option, instead of dropping the visitor at a dead end. (5) **Removed dead `CompleteRegistration`** event (never fired). (6) **Speed promise made honest:** the scan really takes about a minute, so every on-site "on the spot" line (scorecard landing + homepage hero + packages trial) now reads "in about a minute," matching the loading screen. Ads are handled separately and were not touched.
- 2026-07-30: Homepage hero + mobile polish pass. (1) **Hero is now a full-screen landing panel on desktop** — the section fills the whole screen (viewport height minus the 71px sticky header) and restores the original staggered composition: **headline top-left, copy upper-right, showreel video lower-left, and the partner-logo carousel full-bleed along the very bottom**. Nothing is condensed and the red section below never peeks in on landing. Built with a flex column + grid; the video lives in its **own grid row below the copy** (copy = right column, video = left column of the row beneath), so it's always clearly below the text and stays top-anchored. On screens tall enough there's room for everything; on shorter laptops it's intentionally fine for the **logo strip to fall below the fold** — that's preferred over cramming the video up against the copy (the video must never overlap the text). On phones/tablets the hero keeps its original stacked layout. All in `app/globals.css` (`@media (min-width: 992px)` hero block). (2) The showreel's **grow-on-scroll** (trigger `top 50%`) is natural size at rest and grows **down-and-right from a fixed top-left corner** as you scroll (begins ~150–250px in depending on screen height): the video can't scale up over the headline, and because it sits below the copy it never overlaps the text. As it grows, its row grows, so the hero section grows in height and the **logo strip pushes down** with it (matches the original behaviour). Bottom-aligning the video instead dragged it upward over the text on grow, so it's kept top-anchored. The grow (and the row-height pin it uses) is now gated to the desktop layout breakpoint (≥992px) — previously it was only skipped below 767px, so in the 768–991px gap (and on some mobile widths) it ran against the wrong layout and made the already-wide reel visibly **shrink** on scroll (`components/HeroAnimations.tsx`, `app/globals.css`). (3) **Homepage Oxipack proof video** now plays **with sound** when you press play and shows the browser's native controls (volume + fullscreen) while playing (`components/OxipackProofVideo.tsx`). (4) **"How It Works" step numbers** are now white numbers inside a nexubis-red circle, sitting at the **title's height** on the right of each card (centred on the title so they never overlap the copy; `.plan-card-index` in `app/globals.css`). (5) **Stakes picture on mobile** now runs edge-to-edge and **both** cards (Your Product and Competitor) stretch to fill the width — bigger and balanced, no squished Competitor panel (`@media (max-width: 720px)` stakes block). (6) **"Learn more" links hidden** on the "What We Take Off Your Plate" service rows for now, since those detail pages don't exist yet — commented out in `app/page.tsx` so they're easy to bring back. (7) **Dreamlab / blog page** no longer crashes when Sanity isn't configured (e.g. local dev): the blog index used to throw a hard error ("Published Sanity Blog category is missing…") when the Sanity category fetch returned nothing; it now degrades to a graceful empty state and renders the page. Once Sanity has the categories, the normal validating path still runs and still catches genuinely partial data (`lib/blog/get-blog-index-posts.ts`). Note: the intentional **red Value section stays fully red on mobile** (confirmed — it's the brand band, not a bug).
- 2026-07-29: Packages page: removed the three-item reassurance band under the cards as redundant ("one invoice, one team" is already the header subtitle; "two months' notice" is FAQ Q7). Folded "not sure which fits?" in as a new closing FAQ (now 13 questions). Re-pointed the "Still on the Fence?" section at the Scorecard: new copy ("See where your brand actually stands, before you spend anything." + four Credibility-Score benefits) and its "Learn More" button now links to `/scorecard`. Set the packages H1 to Title Case ("One Creative Team. One Flat Monthly Fee.") per the headings convention.
- 2026-07-28: **Packages page rebuilt** (`feat/packages-v2`). Retired the Flex/Custom card; three tiers now (Momentum, Scale, Partner) driven entirely by `lib/packages.ts` and `lib/packages-faq.ts`. Added a currency toggle (EUR base, USD higher, all figures round), kept the original Monthly/Quarterly/Annually toggle (Annually pre-selected) but only Partner discounts now (green "Save €X" pill; the others show "No Discount Applied"). Partner is the "Most chosen" card with the animated flame. Each feature has a hover tooltip (`components/ui/InfoTip.tsx`, Radix Tooltip) carrying the detail, so labels stay short. New FAQ with `FAQPage` structured data generated from the same source as the visible answers. Added reassurance band + closing CTA. New components live in `components/packages/`. Full details and how-to in `docs/architecture/PACKAGES_V2.md`.
- 2026-07-27: Responsive QA pass and mobile fixes. **Header (all pages):** the "Book an application call" button used its full label on phones and pushed the menu button off-screen; it now shows a compact "Book a Call" on small screens (`components/SiteHeader.tsx`, `.nav-cta-label-full` / `.nav-cta-label-short`). **Packages page mobile:** the billing toggle (Monthly/Quarterly/Annually) had fixed widths and clipped "Annually" — it now wraps and fits; the FAQ used a fixed-height two-column layout that squished the questions — it now stacks full-width; and the "Empowering / Dreams." cloud section (single words at 8rem plus off-canvas decorative clouds) overflowed the screen — it now uses smaller headings, full-width stacking, and clips the cloud bleed. All in `app/globals.css`. **Hero showreel (mobile):** it no longer "grows on scroll" — on phones it starts full-width, and the little control button is now a **fullscreen** button (opens the reel fullscreen with sound) instead of a mute toggle; on larger screens it stays the mute toggle (`components/HeroAnimations.tsx`, `app/page.tsx`, `.hero-reel-fullscreen`). **Known perf note:** the homepage never reaches "network idle" because the closing scroll-video uses `preload="auto"` (`components/SuccessStage.tsx`) — it downloads the whole clip on load, which costs mobile data and could hurt load scores; consider `preload="metadata"` if scrub-smoothness allows.
- 2026-07-27: A round of homepage polish across several sections. (1) **Proof quote attribution** now names the person: the red circle stays as a headshot placeholder (swap for Brigitte's photo when it lands), followed by the **DMN Westinghouse logo** (`public/assets/images/dmn-westinghouse.png`), a very subtle vertical divider, then **Brigitte / Marketing Manager**. The quote itself is still the to-collect placeholder. In `app/page.tsx` (`.proof-attribution`) and `app/globals.css`. (2) **Hero fine print** ("Not ready to book?...") is now genuinely small - it was being overridden to 20px by the `.hero-copy p` body rule, so the selector was scoped to `.hero-copy .hero-support` to win, and set to 14px. (3) **"Brand Ready for Every Launch" tile** - the checklist + Published pill are now centred in the tile (they hugged the left before) by wrapping them in a constrained `.vv-launch-stack` and centring it. (4) **Footer redesign** - the three photos above the wordmark are hidden for now (`.footer-media { display: none }`, easy to restore). The giant "nexubis" wordmark now **rises up on scroll from behind the navigation** - a smooth, scroll-linked reveal (its playhead tied to scroll position, not a one-shot pop), rewritten in `components/FooterAnimations.tsx` (no longer uses GSAP for this). The wordmark sits on the same **grey** (`--surface`, #f2f2f2) as the FAQ section above (no seam), and the **navigation block below is now dark** (`#141414`, the same grey as the "holding" cards, not pure black), full-bleed edge-to-edge, with light text and the white Nexubis logo (`NexubisLogo` gained a `color="white"` option). The footer markup was restructured so the dark nav is a full-width band with its content in a container (fixes white gutters/strip). (5) **Stakes "Your Product" ribbon** - the white Nexubis "N" no longer clips out the top of the red bookmark; the cause was a `%` padding on `.vs-badge` (padding-% resolves against the panel's width, not the badge height), swapped for a fixed `0.3rem`.
- 2026-07-27: More "Stakes" picture polish (`components/vignettes/StakesScene.tsx`, styling in `app/globals.css`). (1) The bookmark badge on "Your Product" now shows the white Nexubis "N" mark instead of a star; it was shortened (the ribbon was too long) and the mark recentred so it sits cleanly in the flag body instead of clipping at the top or overrunning the notch. (2) Added a second little pill so both clicks get a label: the Competitor click still pops "Picked on price", and the Your Product click now pops "Branded for ICP" (same style, timed to that second click and placed by Your Product). Badge in `.vs-badge` / `.vs-badge-mark`; pills in `.vs-pill` / `.vs-pill-pain` / `.vs-pill-win`. (Follow-ups same day: both pills now come in a touch earlier; and the clicks are bursts — the Competitor gets 3 quick clicks and Your Product gets 5, each snappier, with the arrow press, ripple ring, and card jiggle firing on every click. The bookmark badge was also reworked to be tied to the card's state: it's shorter, and it now **unfurls down from the top** (a sleek `scaleY` reveal) with the white Nexubis mark fading in just after, exactly when Your Product turns red — and it **shrinks back up and disappears** when the card returns to its normal colour. The N sits centred in the flag body, no longer clipping. Timing in `@keyframes vs-badge` / `vs-star`.)
- 2026-07-27: Polished the three red "Value stack" pictures (`components/vignettes/ValueVignettes.tsx`, motion in `app/globals.css`). **Tile 1 (Buyers See Your Worth):** the winning red card now does a very subtle jiggle while it hovers up top, so it feels alive rather than frozen. **Tile 2 (More Output, Faster Turnaround):** the cards were scooted up and the arrow brought down so everything lines up centred like the designer's mock; the travelling card no longer pops out of existence at the end — it now glides in, settles exactly onto the pile's front card, and fades as the stack gives a little "received it" pop underneath, so it reads as the card joining the pile. **Tile 3 (Brand Ready for Every Launch):** rebuilt to match the design — the category icons now use the filled Nexubis icon library (globe, edit, box, calendar, same set as the Guide cards), the "Published" button uses the filled brand rocket, and the loop is slower and smoother: each row ticks off one at a time (light badge → red badge with a white check), then the Published pill flips from charcoal to red with a quick "clicked" press-and-bounce, holds, and the whole list resets and loops. All loops still hold on the finished state under reduced motion.
- 2026-07-27: Updated the "How It Works" pictures with the designer's real assets. (1) Swapped in the designer's new team headshots — they now appear on the call grid in Step 1 and across the org chart in Step 3, with five distinct faces in the chart (no repeats). The images live in `public/assets/images/avatar-1.png` … `avatar-5.png`. (2) Replaced the little client "company" icon with the designer's building mark (its own grey disc and charcoal building), used both as the person-you're-meeting tile in Step 1 and the client at the top of the org chart in Step 3; the source is `public/assets/images/client-building.svg` and the same shape is drawn inline in `components/vignettes/PlanVignettes.tsx`. (3) Gave Step 3's org chart its own gentle looping animation to match Steps 1 and 2 — after it assembles, a soft red glow now rolls across the team members one at a time, and the Nexubis mark keeps its steady pulse. Reduced-motion still holds on the finished chart. Styling and motion are in `app/globals.css`. (Follow-up same day: the designer sent two more headshots, so `avatar-1.png` and `avatar-4.png` were replaced with them for the Step 1 call grid. Because those files had been shared with the org chart, the org chart was then **decoupled** — its blond team member now uses its own file, `avatar-blond.png`, so future call-grid tweaks never change the org chart again. The Step 3 glow loop was also made much bolder — a solid red ring, an outer bloom, and a clear lift roll across the team so the motion is obvious at a glance. Note: the person who was 3rd in the org chart previously came from the old `avatar-1` image; that file was reused for a call-grid headshot, so the org chart's 3rd slot currently shows the long-haired team member. If a specific different headshot belongs there, drop it in and it can be pointed at its own file too.)
- 2026-07-27: Rebuilt the closing "Picture the next trade show" section into a scroll-played video, and dropped the trade-show clip in (`public/booth-loop.mp4`, 3.6s). The video sits full-bleed behind the section and **plays as you scroll** — scroll down and the footage moves forward, frame by frame, instead of looping on its own. Once you've scrolled it all the way through (around 70% down the section), a soft dark layer fades in over the footage and the paragraph and "Book an application call" button rise in sleekly on top. It's gentle and smooth (the playhead eases toward your scroll position rather than snapping). **To swap the video later:** replace `public/booth-loop.mp4` (optionally add `booth-loop-poster.jpg` for the first-frame image). **For anyone who prefers reduced motion, or has JavaScript off,** the section falls back to a single-screen booth scene with the copy shown normally — nothing is ever hidden. Verified end-to-end (video scrubs 0→full over the first ~72% of scroll, then copy + overlay reveal over the rest). Code in `components/SuccessStage.tsx`, styling in `app/globals.css`. If the section ever feels too long or short to scroll through, the `height: 260vh` on `.success-section.is-scrub` in `app/globals.css` is the single dial to tune.
- 2026-07-27: Replaced the five hand-drawn pictures in the "What We Take Off Your Plate" section (Brand Identity, Website, 3D & CGI, Video & Motion, Trade Show & Print) with the designer's own Lottie animations, one per service. Each animation plays from the start when you open its accordion row and rewinds when you close it, so a service still "assembles" the moment you expand it. For anyone who prefers reduced motion it holds on the finished frame. The animation files live in `public/assets/lotties/` (`solution-brand.json`, `solution-website.json`, `solution-3d.json`, `solution-video.json`, `solution-tradeshow.json`); a small shared player, `components/vignettes/SolutionLottie.tsx`, loads them, and `app/page.tsx` maps each row to its file. To swap any one out later, drop a new `.json` into that folder under the same name. The old code-drawn versions in `components/vignettes/SolutionsVignettes.tsx` are no longer used.
- 2026-07-27: Swapped the icons inside the three "Guide" proof cards (one team / long-term partnerships / we speak industrial) to the designer's official icon library, so they match the brand exactly. That covers all the little glyphs in the team-hub diagram (code, website, palette, 3D box, pen, calendar), the growth-staircase checks and pluses, and the production-loop stations (bag, box, truck, warehouse). The quality-check station in the production loop is now the same plain checkmark used elsewhere, not a check inside a ring. Only the icon shapes changed — the layouts, colours (white on charcoal/red circles), and animations are untouched. All in `components/vignettes/GuideVignettes.tsx`.
- 2026-07-23: Turned on Meta and email, and started a migration checklist. **Meta:** the pixel (browser) and the server-side Conversions API are both live and verified — a real test event reached Meta (`events_received: 1`). The pixel ID was corrected to `885652097948360`. **Email:** Resend is now connected (a test email sent successfully), and new-lead alerts now go to **laine@nexubis.io** (was `hello@`), sent from `alerts@nexubis.io`. **New living doc `docs/go-live-and-migration.md`** lists every key/setting to configure and exactly what changes when the site moves to the real `nexubis.io` domain — built to make that move smooth and hard to get wrong. Still pending: the Funnelr CRM switch-over (waiting on Shannah's tag doc), Laine's cal.com event setup, Turnstile + database keys, and the Meta custom conversions (created in Meta's dashboard).
- 2026-07-23: Wired up the booking, tracking, and lead pieces of the Scorecard funnel (following the funnel playbook now saved at `docs/funnel-audit-checklist.md`). **Booking:** every "Book an application call" button now goes to the real calendar, `cal.com/nexubis/30min`, set in one place (`lib/booking.ts`); the buttons on a finished report also pass along the person's name, their business, and the report link so a booking is tied back to their Scorecard. A new secure endpoint (`/api/cal-booking`) listens for confirmed bookings from cal.com. **Meta (Facebook) tracking:** the Meta pixel is now live sitewide (id `885652097948360`), and the four funnel moments are tracked — starting a check, unlocking a report (a "Lead"), clicking to book, and a confirmed booking ("Schedule") — each also sent server-side for accuracy. The server side stays dormant (does nothing, no errors) until a Meta access token is added. **Funnelr:** fixed three real bugs in the code that talks to Funnelr (the CRM) so it will actually save contacts once switched on. The full switch-over of the live path onto the new clean Funnelr system is deliberately waiting for Shannah's Nexubis tag/scenario doc, so the exact tag names are right first time. **Note:** none of this sends real data yet until the matching keys are set in the hosting settings (cal.com secret, Meta token, Funnelr key) and Laine finishes the cal.com event setup.
- 2026-07-15: Fixed three consistency notes on the red "Value stack" pictures. (1) In "Buyers See Your Worth" the two cards are now exactly the same height — the plain "Built Without Strategy" card no longer sits taller — and the winning "Built By nexubis" card lifts straight up (with a red glow) instead of growing, so the sizes always match. (2) In "More Output, Faster Turnaround" the two card piles were nudged up so they sit centred in the frame rather than low. (3) "Brand Ready for Every Launch" was rebuilt to the designer's layout: the checklist rows now sit directly on the light panel (the white boxes around each row are gone), the grey lines vary in length row to row like real text, and the red "Published" rocket pill now sits at the bottom-left under the list instead of top-right. The completion animation (grey category icons ticking over to red checkmarks, pill going grey to red) is unchanged and still holds on the finished state under reduced motion. All in `components/vignettes/ValueVignettes.tsx` and `app/globals.css`.
- 2026-07-15: Polished the three "How It Works" pictures after designer notes. (1) The rocket in the "See It Before You Commit" browser was redrawn using the designer's exact rocket so it no longer looks broken. (2) That browser's red divider line now sweeps the full width of the window, edge to edge, instead of stopping partway. (3) The "Our Team Becomes Your Team" org chart was made a touch smaller and pulled up so the bottom row of team photos no longer gets cut off, and all three pictures now sit in boxes of the same height. (4) In the "Book an Application Call" video-call grid, the "who's speaking" highlight no longer just flips between one person and the company — it now roams around several team members and the company in turn, like a real call, and the active person gets a soft red ring that grows and fades around their photo like a sound wave. (Team headshots are still the current placeholders; new ones will be swapped in when supplied.)
- 2026-07-15: Replaced the picture in the "Stakes" section ("A great product with a weak brand pays for it every day.") with the designer's new illustration and a looping story in three beats. It shows two stacked product cards — "Your Product" priced €€ and a "Competitor" priced €, both drawn with the same industrial valve line-art so they look identical — plus a tall red "Sales" bar on the right. In the loop: a cursor hesitates between the two cards and clicks the cheaper competitor (it gets a plain grey flash, never red, and a "Picked on price" tag appears) while the Sales bar drains and holds low so the loss lands; then Your Product upgrades its branding — the valve, price, a star badge, the button and a glowing border all turn Nexubis red; then the cursor comes back and clicks Your Product despite the higher price, and the Sales bar fills back up past where it started before the loop repeats. Red only ever means good here (our upgrade and the Sales bar), never the bad click. It lives in `components/vignettes/StakesScene.tsx` with all its styling and timing in `app/globals.css` — the whole loop's speed is one value (`--vs-loop`) and the phase table above the `.stakes-scene` styles maps every step, so the designer can retime it in one place. For anyone who prefers reduced motion the picture holds still on the finished "your brand wins" state.
- 2026-07-15: Rebuilt the three little pictures in the "How It Works" section to match the designer's new mockups, each with its own looping animation. Step 1 ("Book an Application Call") is now a video-call grid, like a Google Meet room: your team and the client each in their own tile with a round photo, and the "who's speaking" red glow flips from your side to the client's and back, with little reaction icons (a thumbs-up, a raised hand) popping up in the corners. The client's tile shows a plain grey building icon so it's clear which one is them. Step 2 ("See It Before You Commit") is a browser window that shows the "before and after": a dull grey wireframe on the left slides into the finished, on-brand version on the right (pink background, red buttons, a red rocket) as a red divider line sweeps across. Step 3 ("Our Team Becomes Your Team") is an org chart that draws itself outward from the red Nexubis mark in the middle — up to the client (the grey building) and down to five team members' photos on red circles, all connected by red lines. The words on the page did not change; only these illustrations did. They live in `components/vignettes/PlanVignettes.tsx` with their styling and motion in `app/globals.css`, and every animation holds still on its finished state for anyone who prefers reduced motion.
- 2026-07-15: Tightened the three "Value stack" pictures to match the designer's mockups more closely and reworked their animations. Tile 1 ("Buyers See Your Worth"): the plain "Built Without Strategy" card now has the bigger, emptier square thumbnail from the design, so it reads as the generic option, and the "Built By nexubis" card lifts more deliberately to win. Tile 2 ("More Output, Faster Turnaround") was rebuilt to the behaviour the designer described: a card lifts off the small left pile, ferries across (past a faint arrow) and lands on the growing fanned pile on the right; the little progress ring in the corner fills across exactly one trip and resets the moment the card lands, then the next card follows — looping forever. Tile 3 ("Brand Ready for Every Launch") is unchanged in layout; its checklist still completes into red checkmarks as the "Published" pill goes from grey to red. All motion still pauses under reduced-motion settings, showing the finished state.
- 2026-07-14: Rebuilt the three little pictures inside the red "Value stack" section (Buyers See Your Worth / More Output, Faster Turnaround / Brand Ready for Every Launch) to match the designer's new mockups, and gave each a gentle looping "before → after" animation. Tile 1 shows a "Built By nexubis" card — pink thumbnail with the red Nexubis mark, a red star bookmark, a red button — that lifts and wins over a flat grey "Built Without Strategy" card. Tile 2 shows a single brief multiplying into a fanned stack of finished pieces, with a small timer ring filling to signal fast turnaround. Tile 3 shows a checklist whose grey category icons (globe, edit, package, calendar) tick over into red checkmarks while a "Published" pill turns from grey to red. The words on the page did not change; only these illustrations did. They live in `components/vignettes/ValueVignettes.tsx` with their styling and motion in `app/globals.css`. The loops are decorative and hold still (showing the finished state) for anyone who prefers reduced motion.
- 2026-07-08: Rebuilt the three "one team / long-term partnerships / we speak industrial" proof cards from small cramped icon tiles into bigger animated diagrams, following the designer's illustrations. The cards sit beside the "We know how it feels..." quote (quote on the left, the three cards stacked on the right), and inside each card the diagram sits on the left with the sentence on its right (on phones the diagram sits above the sentence). The three diagrams (all using the designer's filled icons on dark charcoal circles): (1) six channel icons (code, website, design, 3D, brand, planning) that ride out from behind the red central "team" hub along the connecting lines, growing small-to-big, hold, then shrink and fade back inward before repeating; (2) a rising staircase of charcoal "done" checkmarks with red "+" tops — both the horizontal and the diagonal growth arrows draw out of the bottom-left corner, and the circles scale up as the arrows fill past them, then everything fades and repeats; (3) the designer's full production loop — a funnel (with a red bead that drops quickly onto the first station to start each pass), a roller conveyor with a packaged-box output, and the five stage nodes (bag, quality check, warehouse, truck, box) wired together by a red track; the current eases into view at the first station and flows around the track. Every node sits in the same dark charcoal shade and snaps bright red the instant the current touches it, then eases back to charcoal once it passes (bag → check → box → truck → warehouse); the packaged box also swells a touch as the current runs through it, then it all loops. The animations run at a calm ~6.5–7s pace and all pause under reduced-motion settings, showing the finished diagram instead. The "We know how it feels..." quote also inks in a little lower on scroll now, so the fill is more visible as you reach it.
- 2026-07-08: Fixed the check rejecting a website typed with a trailing dot (for example "dmnwestinghouse.com.", which browsers and copy-paste sometimes add). The address is now cleaned automatically before it is checked, on both the page and the server, so it no longer shows "That website address does not look right."
- 2026-07-08: Full-report (the unlocked report at its shared link) polish. The plain dark bar at the top is gone, replaced by a sticky, branded navigation that stays visible as you scroll: the Nexubis logo on the left, and on the right a "Book an application call" button plus a "Share with your team" button. Share opens a fresh email (Gmail, or your mail app) with the subject, a short summary and the report link already filled in, so you just add who to send it to and hit send. The "Recommended next step" section now uses the same three animated cards as the homepage "How It Works" section. The "first place to fix" section is now on a Nexubis-red background instead of black. And the closing line is bigger, with its two sentences each on their own line.
- 2026-07-08: Fixed a bug where filling in the unlock form with browser autofill showed "Something went wrong with the form." The form has an invisible anti-spam field that only bots should fill; it was named "company", so Chrome's autofill was filling it too and blocking real people. Renamed it so autofill leaves it alone, and told password managers to skip it. Also removed the "(you)" label from the score chips (the red outline already shows which one is yours).
- 2026-07-08: Third Scorecard polish round. On the cover, the two title lines sit closer together. In "First impression", the client's own screenshot is now a large hero shot on its own, with the three competitor screenshots in a smaller, muted row beneath it. The benchmark radar is bigger again, and the competitor lines on it are lighter so the client's red shape stands out more. The "Unlock full report" button in the sticky bar now has a gentle pulsing ring and a soft scale breath to draw the eye (both off under reduced-motion settings), and it no longer loses its button depth while animating.
- 2026-07-08: Second Scorecard polish round from live testing. The report cover now shows the company name in white, bolder and much bigger than the title line (scales down cleanly on phones). In "First impression", the client's own screenshot is unmistakable: red frame, warmer shadow, slightly larger than the competitor shots. In the score section, the overall-score chips went to two lines (name over score), which frees the middle column so the benchmark radar is now much bigger. The unlock form no longer sits at the bottom of the teaser page: the sticky bottom bar is the single unlock surface, its button (and every locked-section "unlock" link) opens the form in a popup, and the button has a soft breathing glow to draw the eye (off under reduced-motion settings).
- 2026-07-08: Scorecard polish round from the first real test run (DMN-Westinghouse), plus three engine fixes. The flow: the website field now shows a green check and border the moment the address looks right; the scan card feels alive (a light sweep travels along the progress bar, and a rotating line tells you what the scan is doing right now, including one personal beat where it names what your site says you make, plus two labelled dad jokes). The teaser: the radar chart no longer clips its category labels and its grid rings are clearly visible; the score dial now sits above a small 0-100 scale showing where the score lands across the three bands (wide 0-59, visible 60-79, narrow 80-100), so "Visible gap" means something; the score chips got a caption explaining who is who; every locked section carries a quiet "unlock the full report" link; and a sticky bar at the bottom of the screen keeps the "Unlock full report" button one tap away on desktop and mobile - it opens the form in a popup, and hides itself when the inline form is on screen. The report cover is now Nexubis red with white type, the company name in ink, and the title flows as one sentence. Engine fixes: (1) reports now address the company by the name written on its own site ("DMN-Westinghouse", not "Dmnwestinghouse"); (2) when the site alone doesn't reveal competitors (niche industries), the tool now finds them through live web search and validates each one - the DMN test now benchmarks against ACS Valves, Roto-Disc and Gericke; (3) an audience gate: sites that are clearly not industrial manufacturers (news sites, shops, agencies) are politely declined before any scanning cost is spent, and anything uncertain still goes through so a real prospect is never locked out.
- 2026-07-08: Made the Scorecard sharper for the industrial buyers we serve, ahead of the first official pass. (1) The auto-detect step now also builds an "industry fingerprint" from the prospect's own site: the industries they serve, certifications they name (like ATEX or EHEDG), their product families, how they sell, and who their likely buyer is. The report copy uses these exact terms, so findings read like they came from someone who knows the industry. The fingerprint is strictly copied from the site, never guessed. (2) The crawler now reads hard facts straight from the pages, with no AI involved: which languages the site offers, the footer copyright year, links to datasheets/CAD files/product selectors, and how many fields the enquiry form actually asks. (3) Two checks were upgraded: "Consistency with LinkedIn" became "Language coverage for export markets", and "Jargon level" became "Path to a quote" (does the enquiry form take a serious buyer seriously?). These are the gaps that actually cost industrial manufacturers sales. (4) The report can now name concrete competitor advantages measured on their sites ("they offer 9 languages; your site offers one") - computed in code from the same measurements on both sides, so it can never be invented. (5) One extra web search checks whether the prospect even shows up when a buyer searches for what they make. Cost impact: about 1 to 3 cents more per fresh run. Every new signal is built so absence of proof is reported as "could not be checked", never as a false accusation.
- 2026-07-08: Reworked the Scorecard entry page. (1) It now uses the same header as the homepage instead of the old dark bar, and there is proper breathing room between the header and the page content (a spacing value that had been silently missing site-wide is now set, so the same gap appears anywhere it was meant to). (2) The form now asks for **only the website**. What the company makes and who their competitors are is worked out automatically from their site, so there is far less to fill in. (3) The entry card was redesigned in the split "hero + card" style, with a preview chart of the five areas we score (Brand, Website, Visuals, Print, Message) sitting above the website box. (4) The headline "How Credible Is Your Brand, Really?" is now Title Case. Behind the scenes: a new auto-detect step reads the site, infers the one-line description and up to three competitors, and the existing engine benchmarks against them; results are cached per website so re-runs stay free and identical.
- 2026-07-08: A round of homepage polish. The hero's "Check your brand's credibility" link is now a proper outline button sitting beside the red one, and the "Not ready to book?" line is smaller fine print. The big headline now flows as one sentence instead of breaking oddly, and every headline on the homepage uses Title Case ("How It Works", "Buyers See Your Worth", and so on). The three outcome tiles now sit on a bold Nexubis-red section background. The big "We know how it feels..." statement greys out and inks in word by word as you scroll, and the Nexubis logo above "We've become the in-house creative team..." was removed. The two busier guide icons (team hub, growth chart) were simplified and flattened to match the conveyor icon's clean style.
- 2026-07-07: Two more visual upgrades in the new soft "glass" style. (1) The three small icon tiles in the "one team / long-term partnerships / we speak industrial" cards are now little glass dioramas: a glowing red team hub with five channel bubbles gently bobbing around it, a rising bar chart whose red growth arrow redraws itself on a loop, and a hopper that pours a stream of red granules onto a conveyor. (2) The dark "A great product with a weak brand pays for it every day" section now has glassy product cards with a sweeping light reflection, a "Your product" glass pill on the first card, and a loop where a cursor glides in and picks the cheaper lookalike, making its red glow bloom. Also fixed a small bug where the "Picked on price" pill had stopped appearing. All motion pauses under reduced-motion settings.
- 2026-07-07: Redesigned the three "How it works" step illustrations (Book an application call / See it before you commit / Our team becomes your team) in the same soft, dimensional style as the outcome tiles. Step 1 now shows the team joined to a red calendar by a flowing dashed arrow, with a cursor that glides in and presses the Book a call button on a loop. Step 2 is a browser with a before/after slider that sweeps back and forth, revealing the branded design over the gray wireframe. Step 3 is a channel list (website, brand, 3D, video, print) where a red highlight walks down the rows one by one, pulsing each checkmark as it passes. All loops pause for visitors who prefer reduced motion. No wording changed.
- 2026-07-07: Redesigned the three outcome-tile illustrations near the top of the homepage (Buyers see your worth / More output, faster turnaround / Brand ready for every launch) to a softer, more dimensional style: floating white cards with deep soft shadows on a subtle blush-tinted panel, matching new reference art. Each one now plays a smooth repeating animation: a cursor glides over and picks the branded product card, a brief card flies across into a growing stack of deliverables while a gauge sweeps, and a checklist ticks itself off before a "Published" button pops in with a light shine. The animations pause automatically for visitors who prefer reduced motion. The words on the tiles did not change.
- 2026-07-07: Built the complete Brand Credibility Scorecard tool (the Section 7 lead generator). New: the `/scorecard` check flow, shareable reports, the team admin, lead capture with Funnelr webhook and team notifications, and the full AI scoring engine behind it. Three real manufacturer sites were scored end to end to tune the output. The homepage entry points now resolve to the working tool when the Scorecard switch is on.
- 2026-07-07: Elevated every homepage section with hand-built illustrations ("vignettes") in the old process-card style — small, realistic fake interfaces drawn in code (no stock images, no icon libraries). The words on the page did not change at all; only the visuals did. New vignettes live in `components/vignettes/`. Each one is decorative and animates gently as you scroll (and holds still if you prefer reduced motion). Two spots wait on real footage and show a tidy placeholder until it arrives: the Oxipack video testimonial (`oxipack-testimonial.mp4`) and the trade-show booth loop behind the closing statement (`booth-loop.mp4`).
- 2026-07-06: Rebuilt the homepage to the locked 12-section wireframe (new industrial-manufacturer positioning and copy). Added `lib/site-config.ts` so every CTA reads from one place, with `BOOKING_URL` pointing at contact for now. Added the Scorecard feature switch. Updated the header CTA and the footer links, social, and sign-off. Retired the old testimonial wall, the old process cards, the old solutions grid, and the "Empowering Dreams" closer from the homepage. The relocated components (`WorkSection`, `TestimonialsCarousel`) remain in the repo for reuse on other pages.
