Implemented the Contact form migration off Webflow.

Files changed:
- app/api/contact/route.ts
- components/ContactTabs.tsx
- app/globals.css
- .env.example
- CONTACT_PAGE_BASE_BUILD_REPORT.txt

What changed:
- Found existing email infrastructure: Resend, already used server-side by scorecard notifications.
- Added controlled endpoint: POST /api/contact.
- Sends notifications to:
  - hello@nexubis.io
  - laine@nexubis.io
- Uses sender display fallback:
  - Nexubis Onboarding <hello@nexubis.io>
- Uses subject:
  - New onboarding message from a client on contact-form
- Sets Reply-To to the visitor's submitted email.
- Sends plain-text and escaped HTML email content.
- Added server validation, unexpected-field rejection, max lengths, email/URL/package validation, honeypot, elapsed-time check, in-memory rate limit, and server-side Turnstile verification.
- Frontend form now submits to /api/contact without page reload, preserves values on errors, clears only after confirmed success, prevents duplicate submits, and uses an accessible live result message.
- Submit remains disabled until Turnstile is configured and returns a token.

Webflow status:
- No Webflow form endpoint is used.
- No Webflow scripts or reCAPTCHA are loaded.
- No no-reply@webflow.com sender is used.
- Production-code search found no Webflow runtime dependency.
- The only matching x-www-form-urlencoded strings are Turnstile verification requests.

Storage:
- Webflow submission storage was not replicated.
- I found no clearly suitable general Contact enquiry storage destination.
- Existing Funnelr/scorecard endpoints are scorecard-specific, so Contact remains email notification only.

Required environment variables:
- RESEND_API_KEY
- NEXT_PUBLIC_TURNSTILE_SITE_KEY
- TURNSTILE_SECRET_KEY
- CONTACT_EMAIL_FROM, optional

Validation:
- Tested mocked endpoint cases:
  - Empty submission
  - Invalid email
  - Invalid URL
  - Missing required fields
  - Invalid package
  - Oversized input
  - Missing spam token
  - Invalid spam token
  - Provider failure
  - Successful mocked delivery
- Checked /contact in Ship Studio at requested desktop, tablet, and mobile breakpoint ranges.
- Confirmed Cal.com tab still renders the approved inline embed.
- Confirmed tab switching does not duplicate the Cal.com embed.
- Confirmed form is intentionally unavailable locally without Turnstile keys.
- Confirmed no browser console errors.

Checks:
- npm run lint: passed, with existing warnings only.
- npm run build: passed, with the existing proof-placeholder build warning.

Remaining human/config requirement:
- Add/confirm production env vars in Vercel:
  - RESEND_API_KEY
  - CONTACT_EMAIL_FROM if a different verified sender is preferred
  - NEXT_PUBLIC_TURNSTILE_SITE_KEY
  - TURNSTILE_SECRET_KEY
